I can have multiple binary crates by placing them inside `scr/bin`.

To create a library crate, I need to create a
`lib.rs` file. I can have as many binary crates as I desire, but I can have only one library crate